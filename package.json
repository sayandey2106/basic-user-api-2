{
  "name": "heliverse",
  "version": "1.0.0",
  "description": "A user can be add, edit, delete the information and create a team by the user with unique domain and availability",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install":"cd client && npm install",
    "client-build": "cd client && npm build",
    "heroku-postbuild":"npm run client-install && npm run client-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mongodb": "^6.3.0",
    "mongoose": "^8.0.1",
    "nodemon": "^3.0.1"
  }
}
